node_modules/
dist/
# ORT WASM assets copied from node_modules at build time — do not commit
public/ort/
.DS_Store
*.log
playwright-report/
test-results/
coverage/
.vite/

# Parent monorepo gitignores **/package-lock.json, but the standalone repo
# needs it for reproducible CI installs (`npm ci`). Negate for this subtree.
!package-lock.json
